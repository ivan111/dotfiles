[user]
    name = ivan111
    email = i@vanya.jp.net
[core]
    editor = vim
[alias]
    st = status
    br = branch
    df = difftool
    dfc = difftool --cached
    mg = mergetool
    ci = commit
    co = checkout

    # %h: abbreviated commit hash
    # %d: ref names, like the --decorate option of git-log[1]
    # %s: subject
    # %cr: committer date, relative
    lg = log --graph -40 --pretty=format:'%C(red)%h%C(reset) %C(yellow)%d%C(reset) %s %C(green)(%cr)'
    lgs = log --stat -3
[diff]
    tool = vimdiff
[difftool]
    prompt = false
[merge]
    ff = false
    tool = vimdiff
    conflictstyle = diff3
[mergetool]
    prompt = false
[credential]
    helper = cache --timeout=36000
[pull]
	rebase = true
